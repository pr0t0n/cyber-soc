{
    admin off
}

:80 {
    encode zstd gzip

    handle /api/* {
        reverse_proxy {$API_UPSTREAM:api:8000}
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
